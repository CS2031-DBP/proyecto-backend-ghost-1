package com.example.proyecto_dbp.Event.domain;

import com.example.proyecto_dbp.Activity.domain.Activity;
import jakarta.persistence.*;

@Entity
@Table(name = "events")
public class Event extends Activity {
    // Ideas para los atributos de Event(?

    // Getters and Setters
}